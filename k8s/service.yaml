apiVersion: v1
kind: Service
metadata:
  name: aceest-gym-service
spec:
  type: NodePort
  selector:
    app: aceest-gym
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30001
      protocol: TCP
      name: http
